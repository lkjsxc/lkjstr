# Crate Inventory

## Purpose

This inventory expands [crate-boundaries.md](crate-boundaries.md) with current
implemented modules, forbidden ownership, tests, and the next edit target for
each Rust crate.

## Crate Rows

| Crate             | Owns                                                                                                                        | Must not own                                                                   | Current implemented modules                                                                                                                                                                                              | Tests                                                                              | Next edit target                                                                               |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `lkjstr-protocol` | Nostr bytes, events, filters, messages, signatures, relay URLs, NIP helpers, upload auth                                    | browser APIs, storage, UI, relay sockets, app state                            | event parsing, canonical IDs, Schnorr checks, signing, NIP-19, NIP-30, NIP-36, NIP-51, NIP-57, NIP-65, NIP-89, NIP-29 tags, NIP-02 follow lists, Blossom, NIP-96, NIP-98                                                 | `cargo test -p lkjstr-protocol`                                                    | expose any remaining TypeScript protocol callers through WASM bridge functions before deletion |
| `lkjstr-domain`   | pure user and workspace records, reducers, public chat state, relay-set records, tab state                                  | browser effects, SQLite statements, WebSocket handles, DOM state               | accounts, relay sets, workspace layout, tab movement, clean startup, recovery, New Tab catalog, tab snapshots, public chat reducer                                                                                       | `cargo test -p lkjstr-domain`                                                      | move signer precondition and account-state transitions out of TypeScript account modules       |
| `lkjstr-storage`  | table manifest, schema records, repositories, row codecs, operation outcomes, retention, repair, inventory, pressure states | Svelte state, browser globals, WebSocket, NIP-07 prompts                       | protected/cache/diagnostic statements, schema hash, tab keys, ledger rows, stats projection, active selector rows, pressure snapshots, protected and cache row codecs, live worker command specs                         | `cargo test -p lkjstr-storage`                                                     | add optimizer, retention, repair, and search/tag lookup command coverage                       |
| `lkjstr-relays`   | relay scheduling, request budgets, leases, page-read keys, progressive snapshots, scoring, route trust                      | browser WebSocket objects, DOM timers, storage statements, UI copy             | send queue, request scheduler, subscription ids, aliases, close tombstones, budget derivation, page-read registry, snapshots, lease fingerprints, owner registry, route groups, ingress classification, scoring          | `cargo test -p lkjstr-relays`                                                      | connect pure relay effects to `lkjstr-web` WebSocket and timer adapters for product reads      |
| `lkjstr-app`      | browser-local composition, startup recovery, query demand, feed reducers, route planning, hydration, jobs, view models      | direct browser APIs, raw SQLite, raw WebSocket objects, Svelte stores          | workspace runtime, tab filtering, query-demand planning, feed windows, scan planning, geometry, fragments, anchors, LOD, follow graph, late merge, Custom Request parser and run planner, surface inputs                 | `cargo test -p lkjstr-app`                                                         | produce one Home feed view model from Rust storage and relay snapshots                         |
| `lkjstr-ui`       | Leptos workspace and real product surfaces                                                                                  | placeholder UI, fake data, protocol parsing, storage statements, relay sockets | partial workspace shell, Welcome, New Tab, Stats inventory and SQLite health, Settings, Accounts, Relay Settings, Upload Settings, lkjstr Log rows, Tweet draft surfaces                                                 | `cargo test -p lkjstr-ui`                                                          | render Rust feed view-model rows for the first Home slice                                      |
| `lkjstr-web`      | WASM entrypoint and browser host adapters                                                                                   | pure domain decisions, protocol meaning, storage policy, relay budgets         | IndexedDB host tests, SQLite worker calls, startup persistence, protected tool hosts, WebSocket and timer foundations, protocol bridge, relay-score bridge, scan-model bridge, feed geometry bridge, follow-graph bridge | `cargo test -p lkjstr-web`, `wasm-pack test --headless --chrome crates/lkjstr-web` | add typed effect loop between Rust relay runtime and browser WebSocket handles                 |
| `lkjstr-xtask`    | repository checks, docs checks, line caps, storage manifest checks, quiet gates                                             | product behavior, browser adapters, UI rendering                               | check-docs, check-lines, check-rust-style, storage manifest docs, SQLite schema docs, quiet rust-wasm and docker verify steps                                                                                            | `cargo test -p lkjstr-xtask`, `cargo run -p lkjstr-xtask -- check-docs`            | keep new ledgers enforced through existing docs and line checks before adding custom rules     |

## Boundary Rule

New product meaning belongs in the lowest crate that can express it without a
browser effect. Browser-only execution remains in `lkjstr-web`, and rendering
remains in `lkjstr-ui` or temporary Svelte view glue.
