<section class="account-storage-safety" aria-label="Account storage safety">
  <p>
    Local account records and local signing secrets are browser-owned SQLite
    worker data when Workers are available. Cache cleanup never deletes them,
    and protected rows are excluded from retention pruning.
  </p>
</section>
