# Library

## Purpose

This directory contains reusable application code shared by routes and tabs.

## Contents

- Domain modules own protocol, runtime, storage, UI, and workspace behavior.
- Components live under `components/`; tab bodies live under `tabs/`.
