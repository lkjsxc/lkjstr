# Storage Maintenance Support

## Purpose

This directory contains shared fixtures for app-level storage maintenance tests.
The helpers build real storage snapshot records, retention candidates, and
repair rows used by `storage_maintenance_test.rs`.
