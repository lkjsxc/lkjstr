# Relay Sets

This module owns pure relay settings records and reducers.

- `types.rs`: serializable relay-set row types and patch/error enums.
- `defaults.rs`: built-in user and discovery relay sets.
- `reducer.rs`: immutable add, patch, remove, restore, and sorting reducers.
